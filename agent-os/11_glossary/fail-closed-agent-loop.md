# fail-closed-agent-loop

Curated pattern: verification or permission failure keeps the agent loop open — no silent completion or unchecked writeback.

→ [[fail-closed-agent-loop]] (`08_patterns/`)

**Cluster:** verification · orchestration

Distinction: [[fail-closed-defaults]] applies to tool metadata defaults.
