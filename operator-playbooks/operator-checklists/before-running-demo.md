# Before Running Demo

- [ ] In repo root: `C:\Dima\Projects\CURSOR\AGENT`
- [ ] Python 3.10+ available (`python --version`)
- [ ] Know which demo and `--scenario` (check runbook)
- [ ] Not editing code simultaneously
- [ ] For real adapters: default to **mock** (no keys) unless intentional
- [ ] Read demo README if first time

Quick command check:

```powershell
python prototypes/review-loop-agent/minimal-demo.py --scenario happy
```
