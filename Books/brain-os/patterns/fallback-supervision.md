# Fallback Supervision

## Statement

RoutingDecision.fallback_mode + execution-supervisor + FALLBACK_TRIGGERED event.

## Maturity

reusable-pattern

## Provenance

`source/Brain OS.docx` v0.2 fallback supervisor
